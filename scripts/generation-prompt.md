Build a complete standalone static marketing website for prospect {{prospectId}}.

This generation stage is unattended and strictly non-interactive. All operator questions and scope choices have already been handled during discovery and recorded in the supplied artifacts. Do not ask the operator or user any questions, request approval, present alternatives for selection, or pause for feedback. Make every remaining design and implementation decision yourself using your best judgment and continue working. If a skill or workflow normally asks the user to choose a visual direction or approve an intermediate result, skip that interaction, choose the strongest direction yourself, and proceed immediately. Do not finish until the website is implemented, its dependencies are installed, its production build succeeds, and the production artifact exists.

Read `../brief.md` as the sole source of business facts. Read `../assessment.md` for the pre-build analysis, `../build-plan.json` for the operator-approved scope, and `../source/discovery.json` for the structured content inventory. The build plan's `routeManifest` is the explicit generated-site scope: implement its routes and collection strategies, use only its listed source pages for those collections, and do not silently add, remove, or collapse planned content. The build plan's `contentCoverage` controls how complete the prospect must be, and its project strategy controls how galleries are represented. This preview must feel like a credible replacement website the business could evaluate as its own, not a thin landing-page pitch.

Read `../assets/manifest.json` and evaluate the local assets in `../assets/`; the manifest records each asset's dimensions, source page, and content type so you can preserve project and gallery relationships. Work only in the current site directory. Create the project yourself—do not use a repo-wide template or shared component library. Prefer React, Vite, and TypeScript unless the brief clearly suggests an even simpler static implementation.

All visitor-facing content must speak entirely in the business's voice. Never tell visitors that this is a prospect, preview, concept, redesign, replacement, generated site, or that testimonials, images, copy, or facts came from an original, source, current, or existing website. Do not include verification notes, provenance disclaimers, implementation commentary, or language about how the site was made. Keep traceability only in internal project files such as `stock-images.md`; it must not appear in rendered pages, metadata, accessibility labels, or other public content.

Authentic source photography is preferable to stock when it is relevant and technically suitable, especially for portfolios, completed work, products, staff, and premises. When the approved plan includes projects, preserve the strongest real project groupings and make them meaningful parts of the customer journey rather than using the images as anonymous decoration. Do not mix photos from different source projects into a single purported project.

Evaluate every raster image against its largest intended rendered size and crop, not just its thumbnail appearance. Inspect its pixel dimensions before use. Do not upscale an image beyond its native resolution; for prominent hero, banner, and full-bleed imagery, target roughly two source pixels per rendered CSS pixel on high-density displays and verify that `object-fit` cropping still leaves enough usable detail. Use responsive image sizes where appropriate. If an otherwise relevant local image will look soft or pixelated in its assigned layout, either give it a smaller presentation or replace it with a higher-resolution Unsplash image that conveys the same subject, mood, or meaning.

If the supplied imagery is low-resolution, poorly composed, visibly dated, watermarked, irrelevant, or insufficient for a polished design, source suitable stock photography from Unsplash. Use stock only as illustrative mood or category imagery: never present it as the prospect's actual staff, customers, premises, products, equipment, or completed work, and avoid visible third-party branding. Prefer specific, business-relevant search terms over generic corporate imagery. If `UNSPLASH_ACCESS_KEY` is available, use the official Unsplash API, keep the credential secret, use the image URLs returned by the API as required by Unsplash hotlinking rules, and provide visible photographer and Unsplash attribution with referral links. If the key is unavailable, you may select images from public Unsplash pages, preserve the photo and photographer source URLs, and include appropriate attribution. Record every stock image used, its photo page URL, and its photographer in `stock-images.md`. Do not use unstable `source.unsplash.com` random-image URLs.

The redesign must be distinctive, polished, responsive, accessible, fast, SEO-ready, and strongly optimized for the business's real conversion goal. You may rewrite and reorganize source copy, but never invent services, testimonials, reviews, certifications, awards, history, people, prices, locations, contact details, statistics, guarantees, or hours. When a fact is absent, omit it. Preserve factual traceability.

The deployed URL will be `/preview/{{prospectId}}/`, so configure the build for that base path and make all local routes and assets work under it. Copy any used downloaded prospect assets into the site project. Run dependency installation and the production build. Fix every build error before finishing. Ensure the production artifact is `site/dist` or document another output in `site-output.json`.
